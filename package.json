{
  "name": "unravel-index-brands",
  "version": "0.3.10",
  "private": true,
  "description": "Unravel Index — brand-licensing vertical. Commercial constraint diagnostic for brand portfolios.",
  "engines": { "node": ">=20" },
  "scripts": {
    "build:demo": "node scripts/build-demo.js",
    "check": "node scripts/check.js",
    "test": "node --test tests/*.test.js",
    "dev": "netlify dev"
  }
}
